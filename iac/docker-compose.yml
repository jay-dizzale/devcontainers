services:
  iac:
    build:
      context: ..
      dockerfile: iac/Dockerfile
      args:
        INSTALL_CLAUDE: ${INSTALL_CLAUDE:-1}
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
        http_proxy: ${http_proxy:-}
        https_proxy: ${https_proxy:-}
        no_proxy: ${no_proxy:-}
    extends:
      file: ../common/base.docker-compose.yml
      service: base
    ports:
      - "127.0.0.1:40000-40099:40000-40099" # high port range for SSO login flow callback forwarding
    volumes:
      - ./.zshrc2:/home/ubuntu/.zshrc2
      - ~/.aws/:/home/ubuntu/.aws
      - ~/.azure:/home/ubuntu/.azure
      - ~/.terraform.d:/home/ubuntu/.terraform.d
      - ~/.spacelift:/home/ubuntu/.spacelift
