services:
  gcc:
    build:
      context: ..
      dockerfile: ./gcc/Dockerfile
      args:
        INSTALL_CLAUDE: ${INSTALL_CLAUDE:-1}
    extends:
      file: ../common/base.docker-compose.yml
      service: base
